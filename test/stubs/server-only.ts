/*
 * `server-only` 의 테스트용 대체 모듈.
 *
 * 원본은 클라이언트 번들에 섞이면 즉시 던지도록 만들어진 마커 패키지입니다.
 * Next 는 서버 빌드에서 이걸 빈 모듈로 바꿔치기하는데, vitest 에는 그 장치가
 * 없어서 서버 전용 모듈을 import 하는 순간 테스트가 죽습니다.
 *
 * 원본 패키지의 exports 맵이 "." 만 노출해서 내부 empty.js 를 직접 가리킬 수
 * 없기 때문에 이 파일을 둡니다. vitest.config.ts 의 resolve.alias 에서 참조합니다.
 *
 * 이 대체가 서버/클라이언트 경계 검사를 무력화하지는 않습니다. 그 검사는
 * `next build` 가 실제 번들링에서 수행하고, 그쪽은 그대로 살아 있습니다.
 */
export {};
